<Window x:Class="InternshipNet.Views.AddInternshipView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create New Internship" Width="400"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">

    <Border Padding="20">
        <StackPanel>
            <TextBlock Text="Job Title:" FontWeight="Bold"/>
            <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" Margin="0,5,0,15" Padding="4"/>

            <TextBlock Text="Company:" FontWeight="Bold"/>
            <!-- Група: Вибір + Кнопка додавання -->
            <Grid Margin="0,5,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <ComboBox Grid.Column="0" ItemsSource="{Binding Companies}" 
                          SelectedItem="{Binding SelectedCompany}" 
                          DisplayMemberPath="Name" Padding="4"/>

                <Button Grid.Column="1" Content="➕" Width="30" Margin="5,0,0,0" 
                        Click="AddCompany_Click" ToolTip="Add New Company"/>
            </Grid>

            <TextBlock Text="Description:" FontWeight="Bold"/>
            <TextBox Text="{Binding Description}" Height="80" TextWrapping="Wrap" 
                     AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                     Margin="0,5,0,15" Padding="4"/>

            <CheckBox Content="Is Remote Position?" IsChecked="{Binding IsRemote}" Margin="0,0,0,20" FontWeight="Bold"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Create" Click="OkButton_Click" IsDefault="True" Width="80" Background="#007ACC" Foreground="White" Height="30"/>
                <Button Content="Cancel" IsCancel="True" Width="80" Margin="10,0,0,0" Height="30"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>