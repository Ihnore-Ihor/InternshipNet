<Window x:Class="InternshipNet.Views.AddApplicationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Apply for Internship" Width="350"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <StackPanel Margin="20">

        <TextBlock Text="Select Student:" FontWeight="Bold"/>

        <!-- Вибір студента + Кнопка додавання -->
        <Grid Margin="0,5,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <ComboBox Grid.Column="0" ItemsSource="{Binding Students}" 
                      SelectedItem="{Binding SelectedStudent}" 
                      DisplayMemberPath="Name" Padding="4"/>

            <Button Grid.Column="1" Content="➕" Width="30" Margin="5,0,0,0" 
                    Click="AddStudent_Click" ToolTip="Add New Student"/>
        </Grid>

        <TextBlock Text="Status will be set to 'Pending'" Foreground="Gray" FontStyle="Italic" HorizontalAlignment="Center" Margin="0,0,0,15"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Apply" IsDefault="True" Click="OkButton_Click" Width="80" Background="#007ACC" Foreground="White" Height="30"/>
            <Button Content="Cancel" IsCancel="True" Width="80" Margin="10,0,0,0" Height="30"/>
        </StackPanel>
    </StackPanel>
</Window>